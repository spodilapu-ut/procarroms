<%= form_with(model: [@team, member], local: true) do |form| %>
  <% if member.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(member.errors.count, "error") %> prohibited this member from being saved:</h2>

      <ul>
      <% member.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :team_id %>
    <!-- <%= form.text_field :team_id %> -->
    <%= form.collection_select :team_id, Team.all, :name, :name %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <!-- <%= form.text_field :user_id %> -->
    <%= form.collection_select :user_id, User.all, :name, :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
