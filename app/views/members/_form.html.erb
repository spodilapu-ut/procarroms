<%= form_with(model: [@team, member], local: true) do |form| %>
  <% if member.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(member.errors.count, "error") %> prohibited this member from being saved:</h2>

      <ul>
      <% member.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="d-flex justify-content-center" class="form-group row">
    <!--<label class="col-sm-2 col-form-label">Team ID</label>
    <div class="col-sm-8">
      <%= form.text_field :team_id, value: @team.name, disabled: true %>
    </div>-->
  </div>
  <div class="d-flex justify-content-center" class="form-group row">
    <label class="col-sm-2 col-form-label">User Name</label>
    <div class="col-sm-8">
      <%= form.collection_select(:user_id, User.all, :id, :name, 
        {:prompt => true}, {:class => 'custom-select mb-3 select-width'})%>
    </div>
  </div>
  <div class="d-flex justify-content-center" class="form-group row">
    <div class="col-sm-10">
      <%= form.submit class: 'btn btn-sm btn-success' %>
      <%= link_to 'New User?', new_user_path, class: 'btn btn-sm btn-secondary'  %>
    </div>
  </div>
<% end %>
